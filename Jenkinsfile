pipeline {
    agent any
    
    stages {
        stage('Clone') {
            steps {
                sh 'git clone https://github.com/Gnomina/WordPress_WIP.git'
            }     
        }
    }
}